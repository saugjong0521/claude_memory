---
name: webapp_responsive_layout
description: 휴대폰+웹을 한 코드로 내는 웹앱(React/Tailwind)의 화면 규격 — 유동형 레이아웃 + 디자인 기준 360 + 최소 폭 344(그 아래는 가로 스크롤) + 태블릿 분기 768, 글자 최소 13px, 한글 어절 줄바꿈, 검수는 344/360/390/820 스크린샷 (2026-09-11 boomerang 손님앱에서 확정)
metadata:
  type: feedback
---

**휴대폰과 웹을 같은 번들로 내는 웹앱(손님용 React/Tailwind 앱, Capacitor 로 감싸도 동일)은 아래 규격으로 만든다.** 사용자가 2026-09-11 boomerang 손님앱에서 "이 방식을 웹앱 할 때 쓰도록" 확정.

## 레이아웃
- **유동형 하나.** 360·720·1080 같은 고정 화면 3단 + 사이 스크롤(2010년대 적응형)은 쓰지 않는다. 폭에 따라 늘어나는 레이아웃 + 최소 폭 하나 + 큰 화면 분기 한두 개.
- **디자인 기준 360px** (갤럭시 S, 한국 시안 관행) — 화면을 그릴 때 이 폭으로 본다.
- **지원 최소 폭 344px** (갤럭시 Z Fold5·6 커버). `body { min-width: 344px } html { overflow-x: auto }` — 그보다 좁으면 UI 를 더 압축하지 않고 344 레이아웃을 가로 스크롤로. 360 을 바닥으로 잡으면 폴드 커버에 항상 16px 스크롤이 생긴다.
- **태블릿·PC 분기는 `md`(768px) 하나**, 필요한 요소에만(일러스트 높이·여백). 폰 중심 앱은 큰 화면에서 컬럼을 늘리지 않고 `max-w-xl`(576px) 가운데 고정.
- 그림·일러스트는 폭이 아니라 **높이를 분기별로 고정**(예 `h-[84px] md:h-[180px] w-auto`) — 폭 기준이면 화면마다 높이가 달라 옆 글자 여백이 어긋난다.

## 글자
- **최소 13px** — Tailwind `fontSize.xs` 를 13px 로 올리고, `text-[9px]`·`[10px]`·`[11px]` 같은 임의 크기는 전부 xs 로. 본문 14(sm), 강조 16(base). 12px 이하는 폰에서 매장명·업종이 안 읽힌다.
- **한글은 어절 단위 줄바꿈** — `body { word-break: keep-all; overflow-wrap: anywhere }`. 화면마다 `break-keep` 을 붙이지 말고 전역 한 줄. `anywhere` 가 URL·이메일 같은 긴 토큰의 가로 넘침을 막는다.
- 탭 라벨은 `whitespace-nowrap` — 라벨 하나가 두 줄로 꺾이면 그 줄의 탭 높이만 달라진다.
- 글자를 키우면 **절대 위치 리본·뱃지가 옆 줄을 덮는다** — 리본은 카드 상단 띠(흐름), 뱃지는 이름 줄이 아니라 보조 줄에. 이름은 `truncate` 대신 `line-clamp-2`.
- **한 문장 안내는 "주문장 / — 설명" 두 줄로** (`<br />— `): "결제 누적 5만원마다 2장 적립" 다음 줄에 "— 친구가 내 코드를 등록하면 …". 체크 목록처럼 항목이 여럿이면 한 줄 유지(항목마다 두 줄로 쪼개면 오히려 산만). 버튼 라벨·짧은 상태 문구는 그대로 (2026-09-11 사용자).
- 라벨은 한 줄에 들어갈 길이로 ("시범 지원 중" → "시범 지원", "🪙 사용 금액 N% KSTA 적립" → "적립율 N%"). 이모지 아이콘은 빼는 쪽.

## 검수 (구현 후 반드시)
- mock 빌드 + Playwright 로 **344 · 360 · 390 · 820** 폭 스크린샷을 탭 전수로 찍고 본다. `document.documentElement.scrollWidth` 가 뷰포트를 넘으면 가로 넘침. 탭 줄 높이가 폭마다 같은지, 글자 중간 꺾임("자정\n에")이 없는지.
- 온보딩·휴대폰 모달 같은 오버레이는 스크립트에서 먼저 닫고 찍는다 (안 닫으면 스크린샷이 전부 모달).

**Why:** 2026-09-11 손님앱 — 12px 글자와 9~11px 뱃지 60곳이 폰에서 안 읽혔고, 글자를 키우자 절대 위치 리본이 매장명을 덮고 라벨이 두 줄로 꺾였다. 344px(폴드 커버)에서는 글자 중간 꺾임과 탭 높이 불균일이 화면마다 있었다. 전역 규칙 3개(최소 폭·글자 스케일·keep-all)로 한 번에 정리됐다.

**How to apply:** 새 웹앱 화면을 만들거나 기존 화면의 글자·폭 문제를 고칠 때 이 규격을 먼저 적용하고, 화면별 예외는 이유가 있을 때만. 관련: [[ui_design_principles]] (화면 구성 원칙), [[verify_against_code_and_runtime]] (스크린샷이 진실).
