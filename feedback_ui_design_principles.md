---
name: ui_design_principles
description: 제품 화면을 만들 때의 원칙 묶음 — 단계별 화면(상태 머신·긴 폼), 소개와 입력 분리, 목록은 표, 문구는 형제 양식, 버튼은 바탕과 구분. 지적받고 고치는 건 실패로 센다
metadata:
  type: feedback
---

제품 UI 를 만들 때 아래를 **구현 전에** 스스로 검사한다. 사용자가 화면마다 지적하게 만드는 것 자체가 실패다
(2026-08-19 "하나하나 주먹구구식으로 다 잡아줘야 되는게 말이 되나?" / 2026-08-25 "fable인데 주변 내용과 비즈니스 흐름 읽고 알아서 하는거 아님?").

**1. 한 화면 = 한 판단.** 상태 머신(신청→심사→진행→평가)이 있으면 상태 × (보여줄 것/할 것/숨길 것) 매트릭스를
먼저 적고, 그 단계의 판단에 필요한 정보·액션만 그린다. 지난 단계 자료는 접기, 다음 단계 자료는 안 그림.
액션 불가 상태는 대체 텍스트 없이 **안 그린다**. 같은 엔티티가 한 화면 두 블록에 반복되면 한 표로 합친다.
(2026-08-19 admin 지역매니저 탭 3연속 지적, 2026-08-20 자기거래·섭외 매장 이중 나열)

**2. 소개와 입력은 분리, 긴 폼은 단계로.** 랜딩/소개 화면에 신청 폼을 욱여넣지 않는다 — 소개 → CTA → 별도 입력
화면. 입력 항목이 한 화면 분량(대략 6개)을 넘거나 성격이 갈리면(매장 정보 / 계정 / 담당자) **스텝 위저드** +
마지막 확인 단계. 단계마다 그 단계 항목만 검증. (2026-08-25 "누가 모집창에 신청 폼까지 UI를 욱여넣음? 단계적으로 해야지")

**3. 목록은 표.** 사람·항목 목록을 칩 가로 이어붙이기(flex-wrap)나 항목당 한 줄 무한 세로 나열로 뿌리지 않는다.
열 정렬 표/그리드 + 그 화면에서 찾는 기준의 정렬 + 조치 불필요 그룹은 count 만 보이는 접기 + 스크롤 상한.
"100명이면 어떻게 보이나"를 자문. 상태 색은 셀 단위. (2026-08-19 하위 명단 2연속 지적)

**4. 문구는 형제 양식.** 라벨·뱃지·안내를 추가하기 전에 같은 자리 형제 항목의 톤·길이·기호를 읽고 따른다.
이모지·유니코드 장식은 기존 컨벤션에 있을 때만; 새로 넣으려면 사용자 확인. 아이콘이 필요하면 인라인 SVG.
운영 내부 용어("검토중 전환")를 손님 문구에 쓰지 않는다. 시스템이 보장하지 않는 약속("직접 찾아뵙고")을 쓰지 않는다.
(2026-08-18 stale 라벨 ⏰ 사건)

**5. 버튼·카드는 바탕과 구분되게, 단 강한 원색 면으로 때우지 않는다.** 옅은 틴트 배경 위의 흰 카드는 테두리(2px 틴트)
+ 부드러운 그림자 + hover 리프트로 "누를 수 있음"을 만든다. "색 구분해서" 는 대개 두 요소를 다른 색으로가 아니라
바탕과 구분되게라는 뜻 — 모호하면 한 줄 확인. (2026-08-25 선택 화면 4회 반복 수정: 흰색→보라 2색→보라 통일→흰 카드+테두리)

**6. 참고 기준.** 잘 만든 SaaS/커머스 랜딩의 선택 카드·위저드 패턴을 기본값으로 삼는다. 기존 앱의 `btn-primary`·`card`
같은 공용 클래스와 브랜드 팔레트 안에서 해결하고, 새 색·새 컴포넌트 스타일은 이유가 있을 때만.

관련: [[check_design_history_before_changes]] (기존 결정 이력), [[explain_with_business_logic]] (설명도 비즈니스 축)
