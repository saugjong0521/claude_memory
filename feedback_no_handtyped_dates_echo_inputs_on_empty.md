---
name: no_handtyped_dates_echo_inputs_on_empty
description: 날짜/연도 리터럴을 손으로 치지 말고 시스템 날짜·컨텍스트에서 파생. 쿼리가 "성공+빈 결과"면 외부 원인 가설 전에 내가 보낸 입력값 echo 검증부터
metadata:
  type: feedback
---

날짜·연도가 들어가는 코드/쿼리/명령을 쓸 때 **연도 리터럴을 손으로 새로 치지 않는다** — 시스템 날짜(`date`, `datetime.now()`)나 대화 컨텍스트의 기존 값에서 파생한다. 그리고 조회가 **"성공 + 빈 결과"** 를 돌려주면, 외부 원인(권한·계정·설정) 가설을 세우기 전에 **내가 실제로 보낸 입력값(파라미터·URL)을 echo 로 재검증**하는 것이 1순위.

**Why:** 2026-08-07 NCP Billing API 검증에서 월 파라미터를 손으로 치다 `202507/202508`(2025년!)로 오타 — 학습 데이터 연도 분포(≤2025 압도적) 탓에 컨텍스트가 2026이어도 즉석 생성 리터럴은 습관 연도로 미끄러진다. API 가 에러 아닌 "성공+0건"을 반환하자 오타 재검증 대신 "인증키 계정 불일치"라는 미관측 외부 가설을 발명해 사용자에게 오진 보고 → 사용자가 "제대로 등록한 건데?"로 교정. 사용자: "다른 세션에서도 비슷한 상황이 나왔었는데" — 반복 패턴 확인됨. [[verify_runtime_supervisor_before_restart]] 의 "negative → positive 비약·미관측 행위자 발명" 안티패턴과 동일 구조.

**How to apply:**
- 스크립트/쿼리에 오늘·이번 달이 필요하면: bash `$(date +%Y%m)`, python `datetime.now().strftime(...)` — 리터럴 금지. 과거 특정 시점이면 대화에 이미 등장한 문자열을 복사.
- 부득이 리터럴을 썼다면 실행 전 현재 날짜와 눈으로 대조 (특히 연도).
- "성공+빈 결과/0건/0원" 응답 → 첫 액션 = 요청 원문(전체 URL·파라미터) 출력해 기대값과 대조. 그 다음에야 권한/계정/설정 가설.
- 재현되는 빈 결과를 근거로 외부 시스템을 의심하는 보고를 쓰기 전, "내 입력이 틀렸을 가능성"을 명시적으로 배제했는지 자문.
