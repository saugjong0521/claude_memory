---
name: verify-edit-applied-before-reporting
description: 치환·삭제·대량 변경은 적용 여부를 재조회로 확인한 뒤 보고. 스크립트의 print("✅") 는 실행 사실일 뿐 적용 근거가 아님. 기계적 대량 삭제는 정규식 대신 파서/괄호 균형 + 린터·런타임 대조
metadata:
  node_type: memory
  type: feedback
---

파일을 바꿨다고 **보고하기 전에** 그 변경이 실제로 적용됐는지 **재조회로 확인**한다. 편집 스크립트가 끝까지 돌았다는 사실은 적용 근거가 아니다 (앵커 불일치 시 `replace` 는 조용히 아무것도 안 바꾸고 넘어간다). 그리고 **대량 기계적 삭제를 정규식으로 하지 않는다** — 괄호 균형을 세는 파서 수준 처리 + 린터·빌드·런타임 대조까지가 한 세트다.

**Why:** 2026-08-12 kstadium-referral 세션에서 같은 뿌리의 실수 2건.

1. **미적용 편집을 완료 보고** — 파이썬 편집 스크립트 끝에 `print("000 ✅")` 를 무조건 실행되게 둠. 앵커 문자열이 실은 **다른 repo 문서의 것**이라 `str.replace` 가 아무것도 안 바꿨는데 "✅" 가 찍혔고, 그대로 사용자에게 "docs/000 갱신 완료" 로 보고. 직후 `git status` 에 그 파일이 없어서 자체 발각 — 사용자가 먼저 봤으면 신뢰를 잃는 종류의 오보.
2. **정규식 대량 삭제로 파일 2회 파손** — mockAdapter.js(1819줄)에서 admin 분기 제거 시 (a) `if (method === 'post' && mRegen)` 처럼 **변수가 조건 뒤쪽에 오는 형태**를 정규식이 놓쳐 선언만 지우고 사용처를 남김 → `no-undef` 12건, (b) `const SHARE_DEMO_ROUNDS = [` 를 **중괄호만 세는** 로직으로 잘라 파싱 불가 상태로 만듦. 둘 다 내 검토가 아니라 **eslint / 파서**가 잡았다. 백업에서 복구 후 괄호·대괄호·소괄호를 함께 세는 방식으로 재작업.

배경: 같은 세션에서 eslint `no-undef` 가 제거 실수를 **3번** 잡았다 (`summaryLoading` 미사용 setter 포함). 린터가 없었으면 전부 런타임에서 터졌을 것.

**How to apply:**

1. **편집 후 즉시 재조회** — `grep -c <새 문구>` / `git status --short` / 해당 줄 재출력 중 하나로 **적용 사실을 눈으로 확인**한 뒤 보고한다. 확인 없이 "갱신 완료" 라고 쓰지 않는다.
2. **성공 메시지를 조건부로** — 편집 스크립트는 `assert old in t` 로 앵커 부재 시 **터지게** 만든다. 무조건 실행되는 `print("✅")` 금지 (실행 로그와 적용 결과를 혼동시킨다).
3. **대량 삭제는 파서 수준으로** — 블록 경계는 `{}`·`[]`·`()` 를 **함께** 세고 문자열 리터럴 안의 괄호는 제외한다. 조건문 매칭은 "변수가 어느 위치에 오든" 잡히게 조건 전체를 본다.
4. **삭제 전 백업 + 삭제 후 3중 확인** — 원본 사본 보관 → ① 린터(미사용/미정의) ② 빌드 ③ **런타임 대조**(제거 전/후 모듈을 각각 로드해 남겨야 할 경로가 동일 응답을 내는지). 정적 검사만으로 끝내지 않는다.
5. **린터 결과를 신호로 대접** — `no-undef`/`no-unused-vars` 는 잔소리가 아니라 이 클래스의 유일한 자동 그물이다. 게이트가 일부 경로만 검사한다면(예: `eslint src`) 그 밖은 사람이 봐야 한다는 뜻.

관련: [[no_guess_in_docs]] (= docs **내용**을 grep 근거로 채움 — 본 룰은 **적용 여부** 확인), [[no_single_source_generalization]] (= build/typecheck 통과 ≠ 런타임 정상), [[docs_as_guide_code_as_truth]].
