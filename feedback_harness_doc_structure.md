---
name: 프로젝트 하네스 (000.harness) 권장 구조
description: 새/기존 프로젝트의 docs/000.*.md 하네스 작성·갱신 시, "프로젝트 역할 → 한눈에 보기 → 시스템 흐름 → 모듈/페이지 시나리오 → 식별자 인덱스 → Gotchas" 순서를 골격으로 사용
type: feedback
originSessionId: f3af43d0-b662-4e0d-a30c-fee4d49d5f94
---
프로젝트 `docs/000.*.md` (= Claude 1차 참조 하네스) 작성·갱신 시 다음 순서를 권장.

**Why:** 신규 진입자 (Claude 또는 사람) 가 코드 작업 전 1차로 읽을 때, "왜 만든 건가" → "어떻게 구성했나" → "어떻게 동작하나" 순서가 자연스러운 mental model 형성을 돕는다. "구현부터 → 의도" 거꾸로 순서면 stack/auth 같은 디테일을 추측 기반으로 읽게 되어 비효율. 다른 프로젝트 (FE/BE) 와도 호칭이 일관 ("본 프로젝트의 역할" / "본 서비스의 역할").

**How to apply (FE/BE 공통 골격):**
- §0 읽는 순서 — 본 문서 + 다른 docs / 외부 BE-FE docs / 배포 docs entry 표
- §1 본 프로젝트 (또는 본 서비스) 의 역할 — 무엇을 하는가, end-to-end 라이프사이클 다이어그램, 주체별 책임 분담표
- §2 한눈에 보기 — 스택 / 네트워크 토폴로지 / 디렉터리 트리 (depth 2) / 페이지 또는 모듈 표
- §3 시스템 흐름 — 인증 / 토큰 라이프사이클 / 핵심 동기화 흐름 (= 가장 자주 막히는 지점)
- §4 페이지별 또는 모듈별 사용자 시나리오 — §1 라이프사이클이 어디서 어떻게 구현되는지 매핑
- §5 UI 카드 매트릭스 (FE) 또는 라우터 인덱스 (BE)
- §6 엔드포인트 인덱스 (FE ↔ BE 매핑 / API 계약)
- §7 상태 / 스토어 / DB 테이블 맵
- §8 훅 / 컴포넌트 / 서비스 카탈로그 (식별자 + "= 한 줄 동작")
- §9 환경 변수 / 빌드·실행 스크립트
- §10 Gotchas — 자주 빠지는 함정, 카테고리화 (인증 / 핵심 비즈니스 / FE 또는 BE 내부 등)
- §11 다른 문서 / 추가 참조 — 외부 repo docs / 배포 docs / 후속 docs (`001.*`, `002.*` …)

**세부 조정:**
- 섹션 번호는 프로젝트 성격에 따라 가감 가능하되 §1 (역할) → §2 (한눈에) → §3 (시스템 흐름) → §4 (시나리오) 골격은 유지.
- "변경 이력" 섹션은 **권장 O — 단 git commit 시점에만 1 entry (= 표의 1행) append**. 작업 중간 단계 (= 같은 commit 안의 여러 turn / 사용자 피드백 반복) 의 매 갱신마다 append 하지 않음. docs 의 변경이력은 git commit 과 분기 단위 동기 — **commit 1번 = 변경이력 표 1행 추가**. entry 안 내용 길이는 commit 의 무게에 따라 자유 (= 짧으면 한 문장, 여러 변경 묶이면 리스트 형태로 길게). 작업 중간엔 docs 본문만 갱신하고, commit 결정 시점에 그 commit 의 의미를 정리해 1 entry 로 append.
- BE 의 경우 §5 "라우터 인덱스" / §13 "v2 라우팅 표" 같이 BE 특화 섹션 추가, FE 의 경우 §5 "UI 카드 매트릭스" / "라벨 매핑" 같이 FE 특화 섹션 추가.
