---
name: all_user_facing_output_in_korean
description: 사용자에게 보이는 모든 출력은 한국어. 서브에이전트·워크플로 프롬프트에도 "보고서는 한국어로" 를 명시한다
metadata:
  type: feedback
---

**사용자 눈에 닿는 모든 출력은 한국어다.** 내 답변만이 아니라 **서브에이전트·워크플로가 내놓는 보고서, 태스크 요약, 커밋 메시지 외 산출물**까지 포함한다.
에이전트를 띄울 때 프롬프트에 **"보고서는 한국어로 작성"** 을 반드시 넣는다 (기본값은 영어다).

**Why:** 2026-09-23 boomerang prod 승격 적대적 검증 — 독립 검증 에이전트 2개를 영어 프롬프트로 띄웠더니 최종 보고서가 영어로 나왔고,
그 hand-back 이 사용자 화면에 그대로 떴다. 사용자: "이게 뭐임? 왜 갑자기 영어로 쓴거야 무슨 문제가 있는지 모르겠음" → 내가 내용(검증 결과)만 설명했더니
다시 "아니 한글로 말해야되는데, **영어로 말한거가 문제**라서 내가 얘기한거임". 즉 사용자가 지적한 것은 발견 내용이 아니라 **언어**였다.
에이전트 보고서는 내가 요약해 전달하는 중간 산출물이 아니라 **사용자가 직접 읽는 화면**이다.

**How to apply:**
- `Agent`/`Workflow` 프롬프트 끝에 한 줄 고정: `보고서는 한국어로 작성한다 (코드·식별자·파일 경로는 원문 유지).`
- 출력 형식을 지정할 때 라벨도 한국어로 (`[발견]/[없음]`, `위치/재현/영향/확신도`). 영어 라벨을 쓰면 본문도 영어로 끌려간다.
- 코드 주석·docs·커밋 메시지 규칙은 기존대로 ([[git_conventions]] — 커밋 메시지는 영어 `(yyyymmdd) 동사_내용`).
- 사용자가 지적했을 때 **무엇을 지적했는지** 먼저 정확히 읽는다 — 내용인지 형식인지 ([[read_user_words_literally]]).

관련: [[read_user_words_literally]], [[confirm_before_large_agent_fanout]] (에이전트 띄울 때 같이 챙길 것), [[explain_code_references]]
