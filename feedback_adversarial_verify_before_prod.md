---
name: adversarial_verify_before_prod
description: 보안·인프라 패치를 prod 에 올리기 전 "적대적으로 제대로 확인" — 자체 공격 재현 + 독립 리뷰 에이전트 1개 조합. 자체 점검은 내 전제를 못 의심한다 (2026-09-01 실측: 에이전트 ≈15만 토큰, 실효 발견 4건 중 1건이 내 수정의 전제 오류)
metadata:
  type: feedback
---

**prod 반영 전 사용자가 "확인 적대적으로 제대로, 문제없다면 prod까지 올리기" 라고 하면, 자체 공격 재현과 컨텍스트 오염 없는 독립 에이전트 리뷰를 한 세트로 돌린 뒤 발견을 고치고 재검증하고 나서 올린다.** 자체 점검만으로 끝내지 않는다.

**Why:** 2026-09-01 kstadium-shop 보안 패치(docs/028 M9·M4·M1·M5·L6·L7). 내 자체 공격(리다이렉트 체인 로컬 재현, 호스트 트릭 11종, 쓰기 경로 전수, 포팅 dry-run)은 오탐 1건(bulk CSV)·보강 1건(8초 상한)에 그쳤는데, 독립 에이전트(general-purpose 1개, ≈15.4만 토큰, 30 tool calls, 네트워크·DB 미접촉 조건)가 **실효 4건**을 찾았다 — 그중 1건은 내가 방금 쓴 수정의 전제("`Image.open` 은 헤더만 읽어 싸다")가 틀린 것(청크 스팸 PNG 로 3.2초 정지). 같은 컨텍스트에서 코드를 쓴 나는 그 전제를 의심하지 못했다. 나머지: 재등록 경로의 L6 미완, Pillow 예외 삼킴으로 상한 검사 무력화, 백업 스크립트 실패 처리.

**How to apply:**
1. 변경 항목마다 **공격 목표를 명시적으로 나열**(우회 저장 경로 전수·SSRF/루프 정지·폭탄·검사 우회·회귀·스크립트 인젝션·기존 데이터 회귀) — 에이전트 프롬프트에 그대로 넣는다.
2. 자체 재현은 **실제 실행**으로: 가짜 리다이렉트 서버, 실제 요청 기록(monkeypatch), 타이밍 측정. "검사한 URL" 과 "요청한 URL" 을 혼동하지 말 것(내 프로브 라벨 오류로 FAIL 오판 1건).
3. 에이전트 1개 = 자유 구간(≤20만) — 조건: read-only, 순수 Python 재현 허용, 서버·DB·외부망 금지, tool call 상한(~50), "FOUND/NOT FOUND + file:line + 재현 입력" 형식. 결과의 재현 스크립트(scratchpad)를 내 재검증에 재사용한다.
4. 발견은 **고치고 같은 재현으로 재검증**한 뒤 plan doc 에 표(발견·수정·재검증)로 남기고, 그 다음에 commit → 포팅 → 배포. 에이전트가 "확인 못 함" 으로 남긴 것은 본문에서 해소(DB read-only 집계 등).
5. 발동 조건 = 사용자가 "적대적/제대로 확인" 을 말했거나, 변경이 입력 검증·파서·외부 호출·업로드·인증처럼 우회 가능성이 있는 보안 패치일 때. 단순 문구·UI 변경엔 불필요.

관련: [[confirm_before_large_agent_fanout]] (감사류 에이전트 비용 실측 — 이번엔 15만/개), [[verify_against_code_and_runtime]] (재현·런타임이 진실), [[plan_stage_no_implementation]] (검증 뒤 배포도 사용자 신호 범위 안에서)
