---
name: sync_test_tools_with_feature_changes
description: "기능/플로우를 변경할 때 그 기능의 dev 테스트 도구(모의 데이터 생성기 등)가 있으면 같은 변경에서 자동으로 함께 갱신하라 — 테스트 도구가 새 절차를 우회하면 절차 테스트가 불가능해진다"
metadata:
  node_type: memory
  type: feedback
---

**기능·플로우를 변경하는 작업에서는 해당 기능의 dev 테스트 도구를 반드시 함께 점검·갱신한다** — 사용자가 시키지 않아도 같은 변경 단위에 포함할 것. 테스트 도구는 실제 유저 경로와 **같은 진입점**을 타야 한다 (내부 함수 직행으로 새 절차를 우회하면 dev에서 절차 검증이 불가능).

**Why:** 2026-08-15 kstadium-shop 영수증 제출 단계식 플로우 작업 — 두 제출 카드를 단계 플로우로 재구성하면서 매장 캐시백 쪽 테스트 도구(TestReceiptTool→processFile)는 자연히 새 플로우를 탔지만, 오늘의 영수증 쪽(OpenReceiptTestTool→submit 직행)은 구 방식대로 **단계를 우회하고 즉시 제출**되는 채로 남겼다. 사용자가 "테스트 도구가 해당 절차가 잘 나오도록 되어있는지 확인"이라고 지적한 뒤에야 수정 — "테스트도구는 기능 업데이트 하는 시점에 자동으로 같이 되야하는거 아님?"

**How to apply:**
- 유저 플로우/기능을 바꾸는 작업의 완료 조건에 **"연관 테스트 도구 grep + 진입점 확인"**을 포함: 해당 컴포넌트/서비스를 참조하는 dev 전용 도구(`Test*Tool`, mock 생성기, `VITE_ENABLE_TEST_TOOLS` 류)를 찾아 새 경로를 타는지 확인.
- 테스트 도구의 콜백은 **유저와 같은 진입 함수**에 연결 (예: 제출 함수 직행 X → 파일 준비/파싱 진입점 O). 우회가 의도라면 주석으로 명시.
- 완료 보고에 테스트 도구 동기화 여부를 한 줄 포함 (도구가 없으면 "해당 없음").
- 관련: [[no_single_source_generalization]] (형제 경로 cross-check — 같은 기능의 병렬 진입점을 전수 확인하는 원칙의 테스트 도구 판).
