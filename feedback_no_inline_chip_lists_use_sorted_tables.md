---
name: no_inline_chip_lists_use_sorted_tables
description: 목록 UI 에서 가변폭 칩 가로 이어붙이기·무한 세로 나열 금지 — 열 정렬 표/그리드 + 의미 있는 정렬 + 스크롤 상한
metadata:
  type: feedback
---

사람 목록·항목 목록을 화면에 뿌릴 때 "이름 · 속성 · 속성" 칩을 가로로 이어붙이거나(flex-wrap), 항목당 한 줄씩 무한히 세로 나열하는 UI 를 만들지 말 것.

사용자 원문 (2026-08-19, 지역매니저 하위 명단 2회 연속 지적):
> "이딴식으로 일렬로 무한히 늘려붙이는 ui는 지양하도록 해. 보기 힘들뿐더러 확인하면서 찾기도 힘듬. 정렬시키고 정리해서 유저면에서 보기 쉽도록 해야지, 사람에게 친화적인 ui를 만들어"

**Why:** 가변폭 칩의 wrap 흐름은 행·열이 안 맞아 눈으로 스캔·비교·검색이 안 된다. 항목당 한 줄 나열은 N=100 이면 화면 100줄. 두 방식 모두 "데이터를 뱉은 것"이지 "사람이 읽는 화면"이 아님.

**How to apply:**
- 속성이 2개 이상인 목록 → **열 정렬 표(또는 고정 그리드)**: 같은 속성은 같은 열, 숫자는 우측 정렬, 헤더 sticky.
- **의미 있는 정렬 필수**: 사용자가 그 화면에서 뭘 찾는지 기준 (예: 조건 미달자는 "조건에 가까운 순" = 곧 통과할 사람 위로). 기본 이름순이라도 정렬 없이 뱉지 말 것.
- **그룹핑 + 접기**: 조치 불필요 그룹(통과자 등)은 count 만 보이는 `<details>` 접기, 조치 대상만 펼침.
- **스크롤 상한**(max-h + overflow-y-auto)으로 N 이 커져도 레이아웃 고정. "100명이면 어떻게 보일까"를 항상 자문.
- 상태 색은 셀 단위 (미달 항목만 강조, 충족 항목은 통과색) — 행 전체 색칠 X.

관련: [[no_emoji_follow_copy_conventions]] (같은 결의 UI 품질 지적 — 형제 양식 확인)
