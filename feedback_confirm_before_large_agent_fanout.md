---
name: confirm_before_large_agent_fanout
description: 단일 작업 에이전트는 5시간 창의 ~30%(≈10~15개) 이하로. ≳10 fan-out 전 예상 규모 고지+컨펌. 모델·모드(Fable+ultracode)는 무죄, 개수가 문제. 세션 한도 보호
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 85504ca1-4b64-48fa-8bb9-59a8abd2fadd
---

ultracode 모드가 켜져 있어도, **대규모 에이전트 fan-out (대략 10개 이상) 워크플로를 띄우기 전에 예상 에이전트 수·토큰 규모를 사용자에게 고지하고 컨펌**을 받는다. 소규모 (에이전트 1~3개) 는 고지 없이 진행 OK.

**최종 운영 방향 (2026-06-12 분석·재현 실험 결론 — 한 줄):** 모델·모드(Fable + ultracode)는 그대로 둬도 됨. **단일 작업이 한 5시간 창의 ~30%(≈10~15 에이전트)를 넘지 않게 + 애초에 에이전트가 필요한 작업인지부터 판단.** 전체 분석 문서 = `/home/crypto/docs/claude_session_token_analysis_20260612.md` (§0 운영 규칙).

**실측 기준선 (Max 5x):**
- 5시간 롤링 창 천장 ≈ **하네스 2.34M tokens** (빈 창에서 크래시한 지점으로 확정 — 추정 아님).
- **16 agent = 787k = 창의 ~34%** (완주) / **52 agent = 2.34M+ = ~110% (크래시)**. → 16개로도 이미 창의 1/3, 안전선이 낮음.
- 개당 비용은 두 실행에서 7~9% 이내로 동일, 총량은 에이전트 수에 **선형** (회계는 정상 — 폭주·캐시thrash·재시도 없음). **유일한 문제 = 작업을 예산보다 크게 잡은 것** (= 빈 창 100%로도 모자란 설계 = 결정론적 크래시).

**판정 = "제품 관점 설계 버그" (회계만 정상):** 무해한 명령 1트가 경고 없이 빈 5시간 창을 통째로 태움 = 합리적 사용자가 기대 안 하는 해로운 결과 → **제품·UX 층에선 버그가 맞다**. "정상"은 토큰 회계(과금 정확·선형)라는 좁은 층에만 해당. **"재현됨 = 결정론적"일 뿐 "올바름"이 아님** — 무해 입력에서 매번 같은 해 = 우연한 글리치보다 더 확실한 설계 버그. 근본 = 하네스 거버넌스 결함(ultracode fan-out 안전장치 동시16/총1000이 사용자 1인 예산보다 한참 위 → 못 지킴 + 사전 경고/예산 인식 부재). 외부에서도 "ultracode Swarm Flood / 앵커링-방지 검증 에이전트 폭주"로 보고된 현상이며, 본 세션에서 실제 발생(52 agent·234만·크래시)했음을 확인. **그래서 "정상이라 괜찮다"로 절대 결론짓지 말 것 — Claude가 사전 고지+규모 상한으로 능동 방어해야 하는 결함.**
- 일반 main loop 작업 ≈ 15k 등가/분 → 창을 8시간분으로 씀. **사용 속도 자체는 문제 없음**, 문제는 fan-out 규모뿐.

**실행 규칙 (6항):**
1. 모델·모드는 문제 아님 — Fable + ultracode 유지 OK.
2. "나눈다 = 더 많이 돌린다"가 아님 — 같은 52개를 4번에 쪼개도 총 토큰 동일 → 한 창 안이면 또 크래시. **개수 자체를 줄여라** (총량을 창에 맞춤, 또는 창 reset 후 이어서).
3. **단일 작업 에이전트 ≤ 창의 ~30% (≈10~15개 상한).**
4. **에이전트 필요성 선판단** — repo/대상이 한 컨텍스트(대략 ≤30만 토큰)에 담기면 main loop 직접 작업이 1/5~1/55 비용·동급 품질. 에이전트는 ① 벽시계 급함 ② 한 컨텍스트 초과 ③ 오염 없는 독립 검증일 때만.
5. **finding당 1:1 검증 금지 → 같은 파일 보는 finding 묶음당 1명** (이번 사고에서 verifier 43개가 폭증의 직접 원인).
6. ≳10 fan-out 전 "에이전트 N개·예상 토큰 X" 고지 후 진행.

**Why:** 2026-06-12 — kstadium-referral-backend 개선점 리뷰 요청에 ultracode 설계대로 52-agent 워크플로 (8 finder + 43 적대검증) 를 띄웠더니 ~14분에 약 234만 tokens 소비, **사용자 세션 한도가 10분 만에 소진** (마지막 18 호출이 session limit 실패). 사용자가 "오늘 세션 처음 쓰는데 과도하게 토큰이 엄청 많이 들어감 — 버그인지 확인" 으로 문제 제기. 시스템 모드가 "토큰 비용은 제약 아님" 이어도 사용자의 실제 세션 한도는 유한한 제약.

**근본 원인 (transcript 실측, msg_id dedupe 후 실호출 526회 — jsonl 의 1,442행은 스트리밍 스냅샷 중복이라 그대로 합산 금지)**: 실 청구 ≈ 2.7M (input 132k + cache_write 2.17M + output 405k). 본체는 **52개의 독립 컨텍스트를 각각 처음부터 셋업·적재한 비용**: ① verifier 43개 = 60% (1.6M) — 각자는 표적 검증 (평균 고유 파일 3.3개) 으로 가볍지만 (개당 ~38k), 개수 × (시스템프롬프트+툴스키마 ~6.6k 고정 오버헤드 + 프롬프트 + 파일 3~4개 + grep) 로 누적. 같은 hot 파일이 최대 10개 에이전트에서 중복 적재 (공유 0). ② finder 8개 = 40% (1.1M) — repo 광역 탐색 (개당 Read+Bash ~35회), tests finder 는 한도로 죽어 결과 소실 (수동 재작업 이중 비용). ③ 캐시 자체는 정상 동작 (TTL 만료/재작성 없음 — 호출 간격 최대 140s). ④ 모드 자체는 무관 — 직후 동급 품질의 020 대조는 단일 agent 123k (1/19) 로 완료.

**How to apply:**
- substantive 작업에서 워크플로를 쓰려면 먼저 1~2문장으로: 예상 에이전트 수 + 대략 토큰 규모 + 세션 한도 영향 → 사용자 OK 후 실행.
- 같은 품질 목표를 더 싸게 달성 가능한 설계 우선: **검증은 finding 당 1:1 금지 → 차원/파일 단위 배치** (같은 파일 읽는 finding 들은 한 검증자가 묶어 처리), finder prompt 에 **tool-call/파일 수 상한 명시**, 직접 read 로 대체 가능한 부분은 main loop 에서 처리 (2026-06-12 의 020 대조는 에이전트 1개 + 직접 read 로 ~12만 tokens — 동급 품질).
- 사용자가 `+NNNk` 토큰 예산을 명시하면 그 안에서 자유 실행.
- 관련: [[no_repeat_decided_questions]] (= 사소한 것까지 묻지는 말 것 — 본 룰은 "대규모 비용" 에만 적용).
