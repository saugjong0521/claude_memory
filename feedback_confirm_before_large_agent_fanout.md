---
name: confirm_before_large_agent_fanout
description: fan-out 판정은 개수가 아니라 예상 토큰 3구간 (≤20만 자유 / ≤60만 한줄고지 / >60만 컨펌). 판정 단위는 배치·워크플로 1개가 아니라 작업 체인 누적 — 소규모 fan-out 연속 실행이 세션 한도를 태움 (2026-06-12 + 2026-07-02 재발)
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 85504ca1-4b64-48fa-8bb9-59a8abd2fadd
---

멀티에이전트 fan-out (Workflow / 대량 Agent) 은 **개수가 아니라 예상 토큰**으로 판정하고, **판정 단위는 개별 워크플로가 아니라 사용자 요청 전체의 작업 체인 누적**이다. ultracode 가 켜져 있어도 이 룰이 우선 — "토큰 비용은 제약 아님"이라는 모드 지침보다 **사용자의 실제 5시간 세션 예산이라는 외부 제약**이 위다.

## 규모별 3구간 (5시간 창 ≈ 213만 가중 토큰 기준)

| 구간 | 작업 전체 예상 토큰 | 무거운 에이전트 환산 | 행동 |
| --- | --- | --- | --- |
| **자유** | ≤ 20만 (창의 ~10%) | ~4개 | 고지 없이 실행. 병렬 검색·컨텍스트 격리·독립 검증 실익이 있으면 주저 없이 사용 |
| **자율** | ≤ 60만 (창의 ~30%) | ~12개 | 실행 전 예상 규모 (에이전트 수·토큰·창 점유율) **한 줄 고지 후 진행** — 확인 대기 불필요 |
| **확인** | > 60만 | 13개+ | 예상 규모 고지 + **사용자 확인 후** 실행 |

- 개당 비용 추정: repo 컨텍스트를 적재하는 **무거운 에이전트 ≈ 4.5만~5만/개** (2026-06-12 실측). 단 **검증(verify)류는 최대 ~9만/개까지 실측** (2026-07-02, Fable) — 검증 에이전트를 finder 휴리스틱으로 추정하면 2배 과소평가.
- 스코프 좁은 가벼운 에이전트는 낮게 추정 가능하되, 왜 낮은지 말할 수 있어야 함.

## 실행 규칙

1. **판정 단위 = 작업 체인 누적 (2026-07-02 사고의 핵심).** "나눈다 ≠ 줄인다" — 같은 작업을 여러 워크플로/여러 phase 로 쪼개도 총 토큰 동일. 다단계 요청 (예: docs 제작 → 버그 분석 → 플랜) 이면 **체인 전체의 누적 예상**으로 구간을 정하고, 워크플로 하나 끝날 때마다 **실측 누적 (결과의 totalTokens 합산) 을 갱신해 다음 fan-out 전에 잔여 예산 대비 재판정**한다. 각 fan-out 이 "10개 이하"라도 연속 3개면 누적 20개 = 확인 구간.
2. **에이전트 필요성 선판단.** 대상이 한 컨텍스트 (대략 ≤30만 토큰) 에 담기면 main loop 직접 작업이 1/5~1/55 비용·동급 품질. 에이전트는 ① wall-clock 급함 ② 한 컨텍스트 초과 ③ 오염 없는 독립 검증일 때만.
3. **무상한 fan-out 구조 금지 (2026-06-12 직접 원인).** 런타임 데이터 (finding 수 등) 가 에이전트 수를 결정하게 두지 말고 스크립트에 명시적 cap 을 코드로 박는다. 실행 전 최대 규모를 말할 수 없는 설계 = 결함.
4. **1:1 검증은 상한 안에서만.** finding 수가 상한을 넘길 수 있으면 파일·주제 묶음당 검증자 1명으로 전환 (2026-06-12 의 verifier 43개 폭증 원인 = 상한 없는 1:1).
5. **검증 패스 실익 판단.** 이미 read 근거가 있는 산출물의 전수 재검증 워크플로는 고비용 (2026-07-02: 375주장 검증 = 45.5만 = 그 세션 최대 단일 지출). 고위험 행만 main loop spot-check 하는 대안을 먼저 검토.
6. 사용자가 `+NNNk` 예산을 명시하면 그 안에서 자유 실행 (`budget.remaining()` 가드 병용).

## 실측 사고 기록

**2026-06-12 (1차):** kstadium-referral-backend 리뷰에 52-agent (8 finder + 43 1:1 verifier) 워크플로 → ~14분에 ~234만 토큰, 빈 5시간 창 통째 소진+크래시. 16ag=787k=창의 34% (완주) / 52ag=110% (크래시). 개당 비용 선형·회계 정상 — 문제는 규모 설계뿐. 직후 동급 품질 작업을 단일 agent 12.3만 (1/19) 로 완료. 전체 분석 = `docs/claude_session_token_analysis_20260612.md`.

**2026-07-02 (2차 — 개수 룰의 구멍 실증):** hashcow.publicsale.io 에서 docs 제작→버그 분석 다단계 요청에 워크플로 3개 연속: ①프로젝트 파악 6ag=29.0만 → ②docs 375주장 검증 5ag=45.5만 → ③버그 헌트 9ag=53.0만 (진행 중 세션 한도 도달, 5ag 사망 — CFG finder 는 7.1만 소비 후 결과 소실 = 이중 비용). 서브에이전트 누적 **127.5만 (창의 ~60%)** + 메인루프 → 사용자 세션 한도 소진 (체감 20분). **각 fan-out 은 ≤10개라 기존 "개수 상한" 룰을 전부 통과** — 배치 단위 판정이 구멍. 검증 에이전트 실측 개당 최대 9.1만 (휴리스틱의 ~2배) 도 과소추정 원인.

**판정 = 제품 관점 설계 버그 (회계만 정상), "재현됨 ≠ 올바름", "정상이라 괜찮다" 금지** — 하네스가 예산 연동 안전장치를 제공할 때까지 Claude 가 사전 고지+누적 상한으로 능동 방어해야 한다. 사용자가 토큰 손해를 물으면 계측 정확성으로 변호하지 말고 그 지출이 정당했는지부터 답한다 ([[answer_the_loss_not_the_accounting]]).

## How to apply

- 워크플로 설계 전: 에이전트 수 × 개당 추정 (무거움 4.5~5만, 검증류 ~9만) = 예상 토큰 → 3구간 판정. **다단계 작업이면 체인 전체 합산으로 판정.**
- 각 워크플로 종료 시: 결과의 totalTokens 를 누적 합산 → 다음 fan-out 전 "지금까지 X만 소비, 이번 예상 Y만, 누적 Z만 (창의 N%)" 로 재판정. 자율 구간을 넘어서면 진행 중이라도 멈추고 컨펌.
- 같은 품질을 싸게: 검증은 묶음 배치, finder prompt 에 tool-call/파일 수 상한 명시, 직접 read 로 되는 부분은 main loop.
- 프로젝트 워크스페이스에 `agent-fanout.md` 같은 로컬 룰 파일이 있으면 그것이 최신 개정일 수 있음 — 충돌 시 로컬 파일 우선 후 본 메모리 동기화.
- 관련: [[no_repeat_decided_questions]] (사소한 것까지 묻지 말 것 — 본 룰은 대규모 비용에만 적용), [[answer_the_loss_not_the_accounting]].
