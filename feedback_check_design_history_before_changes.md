---
name: 기존 로직 변경 전 과거 결정 이력 탐독·충돌 고지
description: 이미 구현된 코드에 기능을 추가/변경할 때, 대상 모듈의 docstring·주석·docs 의 "사용자 결정" 이력을 먼저 읽고, 새 요청이 기존 결정과 어긋나면 구현 전에 "기존엔 ~로 설계돼 있었다 — 바꾸는 건데 괜찮냐"로 확인받을 것
type: feedback
---

이미 구현된 코드를 변경/확장하기 전에, **그 코드가 왜 지금 모습인지의 결정 이력을 먼저 탐독**하고, 새 요청이 과거 사용자 결정과 충돌하면 **구현 전에 충돌을 고지하고 확인**받는다.

**Why:** 2026-08-16 kstadium-shop 사건 — "지난 이벤트 수령 리워드 이력 조회" 요청에 바로 구현했는데, 대상 모듈 (referral_events.py) docstring 에 **2026-08-03 사용자 결정 원문** ("추적은 백업을 통해 확인", "데이터를 event_id 별로 쌓아두지 않는다 — 리셋 시 삭제") 이 명시돼 있었다. 심지어 같은 세션에서 그 docstring 을 이미 읽은 상태였는데도 충돌 고지 없이 구현 → **사용자가 스스로 과거 지시를 기억해내 "이전에 삭제하도록 지시했던 것 같은데 확인해줘"라고 되물어야 했다.** 사용자 원문: "이런 경우 이력을 너가 읽고 나에게 ~~~ 이렇게 설계되있었다 기존 로직 바꾸는건데 괜찮냐 식으로 확인을 받았어야 함".

**How to apply:**

1. **기능 추가/변경 착수 전 이력 탐독 (대상 범위):**
   - 대상 모듈/함수의 docstring·주석 — 특히 "사용자 결정", 날짜, 결정 원문 인용 (이 코드베이스 컨벤션상 결정은 코드 주석에 원문으로 남아 있다)
   - docs/000 의 관련 § + 변경이력 표에서 해당 기능 검색 (기능명/파일명 grep)
   - 관련 plan doc (docs/0NN) 이 있으면 결정 섹션
2. **충돌 판정과 고지:** 새 요청이 기존 결정의 취지와 어긋나면 (예: "삭제해서 조회 안 되게" ↔ "조회 화면 추가") — 구현하지 말고 먼저:
   > "이 부분은 YYYY-MM-DD 에 '〈결정 원문 인용〉' 으로 설계돼 있습니다. 이번 요청은 그 로직을 바꾸는 것인데 진행할까요? (선택지: 개정 / 원결정 유지 / 절충)"
3. **충돌이 없으면 그대로 진행** — 모든 변경에 과거 확인을 묻는 게 아니다 ([feedback_no_repeat_decided_questions](feedback_no_repeat_decided_questions.md) 와 균형). 묻는 조건 = "기존 결정과 방향이 어긋날 때"만.
4. **개정 확정 시 기록:** 결정이 바뀌면 docstring 과 docs 양쪽에 "구 결정 → 신 결정 (날짜, 사용자 원문)" 으로 개정 이력을 남긴다 ([feedback_preserve_decision_literal](feedback_preserve_decision_literal.md)).

관련: [[docs_as_guide_code_as_truth]] (코드 주석의 결정 인용 = 1차 사료), [[preserve_decision_literal]], [[no_repeat_decided_questions]].
