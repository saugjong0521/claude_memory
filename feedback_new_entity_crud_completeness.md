---
name: new_entity_crud_completeness
description: "새 엔티티/기능 구현 시 스펙 문장 최소 구현으로 끝내지 말 것 — ① 엔티티 생성 경로 전수 grep ② 운영 CRUD 매트릭스 (생성/조회/수정/삭제 × 누가/어느 화면) ③ 미구현 셀은 완료 보고에 명시 ④ 관리 기능은 운영자가 실제 쓰는 기존 화면에 통합 검토"
metadata:
  type: feedback
---

**새 엔티티 (테이블/코드/자원) 를 도입하는 기능을 구현할 때, 스펙 문장에 명시된 동작만 만들고 완료로 보고하지 말 것.** 엔티티의 수명주기 전체를 설계 대상으로 본다.

**Why:** 2026-08-15 hashcow-publicsale 레퍼럴 구현 — 스펙 "레퍼럴 코드 커미션 할인율 및 지급율 기본 5%, 어드민에서 유저별로 변경 가능" 을 문자 범위 (요율만) 로 구현. 결과 3가지 누락을 사용자가 후속 라운드에서 지적:
① 레퍼럴 **코드 자체의 수정** 기능 부재 (새 엔티티인데 생성만 있고 수정 수단 없음)
② 계정 생성 경로 전수 grep 안 해서 admin unmatched-resolve 의 `manual:` 계정이 코드 미발급 (2차 리뷰에서야 발견)
③ 요율 수정을 새 Referral 탭에만 배치 — 운영자가 유저를 관리하는 동선은 **Accounts 탭**인데 동선 시뮬레이션 없이 새 화면에 몰아넣음
사용자: "이런것들은 작업 요청했을때에 프로젝트에 따라 자동으로 설계했을 법도 한데 못한 이유는?"

**How to apply:**
1. **생성 경로 전수 grep** — 새 엔티티에 부수 자원 (자동 발급 코드 등) 이 붙으면, 그 엔티티의 생성처를 전부 grep (`Account(` 처럼 생성자 호출 검색) 해서 모든 경로에 발급 로직을 태운다. 대표 경로 1~2곳만 하고 끝내지 말 것.
2. **운영 CRUD 매트릭스** — plan doc 에 표로: 새 자원의 생성/조회/수정/삭제 각각을 누가 (유저/어드민/시스템) 어느 화면·API 에서 하는지. 빈 셀 = 의도적 미구현인지 누락인지 판정.
3. **미구현 셀 명시 보고** — 스코프 절제로 뺀 기능은 완료 보고에 "X 는 미구현 (필요 시 추가)" 로 명시해 사용자가 결정하게 한다. 조용히 생략 금지.
4. **관리 동선 통합** — 관리 기능은 새 탭/화면을 만들기 전에, 운영자가 그 대상을 이미 관리하는 기존 화면 (계정 관리 탭 등) 에 통합할지 먼저 검토. "이 버튼을 실제로 누를 사람이 어느 화면에 있나" 를 그려본다.

관련: [[preserve_decision_literal]] (스펙 원문 보존 ≠ 원문 밖 전부 배제), [[cross_repo_audit_before_changes]] (변경 전 매트릭스 점검의 형제 룰), [[no_single_source_generalization]] (대표 1곳 확인으로 전체 단정 금지 — 생성 경로 전수 grep 의 근거).
