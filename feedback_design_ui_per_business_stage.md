---
name: design_ui_per_business_stage
description: 상태 머신이 있는 화면은 처음부터 단계별 판단 대상 기준으로 설계 — 전 단계 공용 UI 깔고 지적받을 때마다 한 조각씩 고치는 방식 금지
metadata:
  type: feedback
---

워크플로/상태 머신(신청 → 심사 → 진행 → 평가 같은)이 있는 기능의 화면을 만들 때, 모든 상태에 같은 목록·같은 컬럼·같은 버튼을 깔아 두고 지적받을 때마다 한 조각씩 고치지 말 것. **처음부터 상태별로 "이 단계에서 운영/유저가 내리는 판단이 무엇인가"를 묻고, 그 판단에 필요한 정보·액션만 그 단계 화면에 넣는다.**

사용자 원문 (2026-08-19, admin 지역매니저 탭 — 같은 날 세 번째 같은 계열 지적 끝에):
> "비즈니스 로직에 따라서 알아서 보일것들 보이고 정리해야 되는데 왜이래 admin 회사쪽에서 지역매니저 관리가 쓰래기같이 만들어 뒀어. 비즈니스 로직 다시 보고 싹 변경해"
> "처음에는 모든데서 다 똑같은 ui 보여줘서 하나씩 변경하고 있는데, 너한테 비즈니스 기획등 다 설명했잖아. 이렇게 하나하나 주먹구구식으로 다 잡아줘야 되는게 말이 되나?"

**Why:** 단계가 다르면 질문이 다르다 (심사 단계 = "승인할 것인가", 등록 단계 = "어디까지 됐나", 활동 단계 = "성과가 나나"). 공용 UI 는 어느 단계에서도 그 질문에 정확히 답하지 못하고, 지난 단계의 정보(자격 스냅샷·심사 버튼)와 아직 아닌 단계의 정보(섭외·연결)가 섞여 노이즈가 된다. 사용자가 화면마다 지적하게 만드는 것 자체가 실패.

**How to apply:**
- 구현 전에 상태 × (보여줄 것 / 할 수 있는 것 / 숨길 것) 매트릭스를 먼저 적는다. plan doc 이 있으면 그 §에 넣는다.
- 지난 단계에서 판단이 끝난 자료는 접기(`<details>`)나 한 줄 요약으로 내리고, 다음 단계 자료는 아예 렌더하지 않는다.
- 진행 단계 화면은 "완료 조건 체크리스트"(N/M + 다음 행동 버튼) 형태가 기본.
- 액션 불가 상태는 대체 텍스트로 채우지 말고 **안 그린다** (자리 차지 + 줄바꿈 유발). 이유 표기는 게이트 버튼 옆 한 곳에만.
- 사용자에게 이미 들은 기획(단계 의미·누가 무엇을 하는지)이 있으면, 새 화면·새 필드를 붙일 때마다 그 기획에 비춰 "이 단계에 이게 보이는 게 맞나"를 스스로 검사한다 — 지적받고 고치는 건 실패로 센다.
- **점검 단위는 편집한 블록이 아니라 화면 전체**: 한 섹션을 고쳤으면 그 상태에서 화면에 함께 렌더되는 다른 블록까지 훑어, **같은 엔티티(매장·유저 목록 등)가 두 블록에 반복 나열되면 매 엔티티당 한 행으로 합친다** (요약 수치는 위에, 상세는 통합 표 한 곳에만). 2026-08-20 재발: 단계별 재구성 직후에도 자기거래 점검 매장 10곳 + 섭외 매장 10곳이 같은 화면에 연달아 나열 — 룰을 만들고도 편집 범위만 보고 화면 전체를 안 봐서 뚫렸다.

관련: [[no_inline_chip_lists_use_sorted_tables]] (목록 형태), [[check_design_history_before_changes]] (기존 결정 이력), [[explain_with_business_logic]] (설명도 비즈니스 축)
